{
  "testSuiteMeta": {
    "title": "UCTO Product-Level Test Suite v2",
    "version": "2.0",
    "targetCoverage": "≥ 95% requirement coverage",
    "generatedAt": "2026-05-13",
    "totalRequirements": 42,
    "totalTestCases": 78
  },
  "uctoTestCases": [
    {
      "id": "TC-API-001",
      "title": "JWT login returns access + refresh tokens",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-001"],
      "targetComponents": ["AuthController", "JwtService"],
      "preconditions": ["Registered user exists in database"],
      "steps": ["POST /api/auth/login with valid email/password", "Extract tokens from response", "Call protected endpoint with access token"],
      "expectedResult": "200 OK with accessToken and refreshToken; protected call succeeds",
      "priority": "High"
    },
    {
      "id": "TC-API-002",
      "title": "JWT refresh extends session",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-001"],
      "targetComponents": ["AuthController", "JwtService"],
      "preconditions": ["Valid refresh token"],
      "steps": ["POST /api/auth/refresh with valid refresh token", "Verify new token pair returned", "Old refresh token invalidated"],
      "expectedResult": "New access + refresh tokens issued; old token rejected on reuse",
      "priority": "High"
    },
    {
      "id": "TC-API-003",
      "title": "Google OAuth login creates or links account",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-001"],
      "targetComponents": ["AuthController", "OAuth2Config"],
      "preconditions": ["Google OAuth client configured", "Valid Google ID token"],
      "steps": ["POST /api/auth/oauth with Google ID token", "Verify JWT returned", "Verify user exists or was created"],
      "expectedResult": "JWT issued; user record exists",
      "priority": "High"
    },
    {
      "id": "TC-API-004",
      "title": "OTP password reset flow works end-to-end",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-001"],
      "targetComponents": ["AuthController", "SmsService", "EmailService"],
      "preconditions": ["User exists with phone number"],
      "steps": ["POST /api/auth/forgot-password", "POST /api/auth/verify-otp with correct OTP", "POST /api/auth/reset-password with new password", "Login with new password"],
      "expectedResult": "OTP sent; verified; password reset succeeds; new password works",
      "priority": "High"
    },
    {
      "id": "TC-API-005",
      "title": "RBAC: Founder can modify project, Viewer receives 403",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-002"],
      "targetComponents": ["ProjectController", "SecurityConfig"],
      "preconditions": ["Two users: FOUNDER and VIEWER", "Project owned by FOUNDER"],
      "steps": ["Founder PUT /api/projects/{id} — expect 200", "Viewer PUT /api/projects/{id} — expect 403", "Viewer GET /api/projects — expect own projects only"],
      "expectedResult": "Founder can modify; Viewer blocked on write operations",
      "priority": "High"
    },
    {
      "id": "TC-API-006",
      "title": "Project CRUD: create, read, update, delete",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-003"],
      "targetComponents": ["ProjectController", "ProjectService"],
      "preconditions": ["Authenticated FOUNDER user"],
      "steps": ["POST /api/projects — create", "GET /api/projects/{id} — read", "PUT /api/projects/{id} — update", "DELETE /api/projects/{id} — delete", "GET /api/projects/{id} — verify 404"],
      "expectedResult": "All CRUD operations succeed; deleted project returns 404",
      "priority": "High"
    },
    {
      "id": "TC-API-007",
      "title": "Project member management add/remove members",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-003"],
      "targetComponents": ["ProjectService", "ProjectMemberRepository"],
      "preconditions": ["Project exists", "Another user exists to invite"],
      "steps": ["Add member with DEVELOPER role", "GET project members — verify member present", "Remove member", "GET project members — verify removed"],
      "expectedResult": "Members added and removed correctly; roles enforced",
      "priority": "Medium"
    },
    {
      "id": "TC-API-008",
      "title": "Repo config GET/PUT with valid data",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-004"],
      "targetComponents": ["ProjectController", "ProjectService"],
      "preconditions": ["Project exists", "Authenticated owner"],
      "steps": ["PUT /api/projects/{id}/repo with valid config", "GET /api/projects/{id}/repo", "Verify all fields match"],
      "expectedResult": "Config saved and returned correctly",
      "priority": "High"
    },
    {
      "id": "TC-API-009",
      "title": "Repo config validation: provider without URL returns 400",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-005"],
      "targetComponents": ["ProjectService"],
      "preconditions": ["Mock ProjectRepository returns existing project"],
      "steps": ["Call updateRepoConfig with repoProvider=GITHUB and empty repoUrl", "Expect RepoValidationException"],
      "expectedResult": "Exception thrown with 'repoUrl must be non-empty when repoProvider is set'",
      "priority": "High"
    },
    {
      "id": "TC-API-010",
      "title": "Repo config validation: invalid provider returns 400",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-005"],
      "targetComponents": ["ProjectService"],
      "preconditions": ["Mock ProjectRepository returns existing project"],
      "steps": ["Call updateRepoConfig with repoProvider=INVALID_PROVIDER", "Expect RepoValidationException"],
      "expectedResult": "Exception thrown with 'Invalid repoProvider'",
      "priority": "High"
    },
    {
      "id": "TC-API-011",
      "title": "Gate status endpoint returns correct structure",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-006", "UCTO-REQ-040"],
      "targetComponents": ["ProjectController", "QualityGateService"],
      "preconditions": ["Project exists", "At least one GateEvaluation record exists"],
      "steps": ["GET /api/projects/{id}/branches/main/gate-status", "Verify response has testGate, complianceGate, coordinatedGate, overallPass"],
      "expectedResult": "All three gate types present with evaluation details",
      "priority": "Medium"
    },
    {
      "id": "TC-API-012",
      "title": "Latest test results endpoint returns data",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-007"],
      "targetComponents": ["SimulatedSprintController", "TestResultRepository"],
      "preconditions": ["TestResult record exists for project+branch"],
      "steps": ["GET /api/projects/{id}/branches/main/test-results/latest", "Verify response has testsRun, testsPassed, status etc."],
      "expectedResult": "Latest test result returned with correct fields",
      "priority": "Medium"
    },
    {
      "id": "TC-API-013",
      "title": "Latest compliance results endpoint returns data",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-007"],
      "targetComponents": ["SimulatedSprintController", "ComplianceResultRepository"],
      "preconditions": ["ComplianceResult record exists for project+branch"],
      "steps": ["GET /api/projects/{id}/branches/main/compliance-results/latest", "Verify response has overallStatus, severity"],
      "expectedResult": "Latest compliance result returned with correct fields",
      "priority": "Medium"
    },
    {
      "id": "TC-API-014",
      "title": "Bootstrap API creates project and skeleton",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-008"],
      "targetComponents": ["BootstrapService", "ProjectService"],
      "preconditions": ["Authenticated user", "Template directory exists"],
      "steps": ["POST /api/orchestrator/bootstrap with prompt and stack", "Verify project created", "Verify workspace backend/ and frontend/ exist"],
      "expectedResult": "Project created; skeleton generated with correct structure",
      "priority": "High"
    },
    {
      "id": "TC-API-015",
      "title": "Audit log created on user action",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-009"],
      "targetComponents": ["AuditLogService"],
      "preconditions": ["Mock AuditLogRepository"],
      "steps": ["Call auditLogService.log(userId, projectId, 'LOGIN', 'details', 'ip', true)", "Verify repository.save called with correct action and success=true"],
      "expectedResult": "AuditLog saved with correct fields",
      "priority": "High"
    },
    {
      "id": "TC-API-016",
      "title": "Audit log records simulation flag",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-009", "UCTO-REQ-038"],
      "targetComponents": ["AuditLogService"],
      "preconditions": ["Mock AuditLogRepository"],
      "steps": ["Call log(..., simulation=true)", "Verify saved AuditLog has simulation=true"],
      "expectedResult": "Simulation flag persisted correctly",
      "priority": "High"
    },
    {
      "id": "TC-API-017",
      "title": "Usage metering blocks agent run at tier limit",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-010"],
      "targetComponents": ["AgentOrchestrationService", "SubscriptionService"],
      "preconditions": ["SubscriptionService.canRunAgent() returns false"],
      "steps": ["Mock subscriptionService.canRunAgent(userId) → false", "Call triggerAgent()", "Expect AgentRunLimitExceededException"],
      "expectedResult": "Exception thrown; no event published; no usage recorded",
      "priority": "High"
    },
    {
      "id": "TC-API-018",
      "title": "Usage metering increments counter on agent trigger",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-010"],
      "targetComponents": ["AgentOrchestrationService", "UsageMeterService"],
      "preconditions": ["SubscriptionService.canRunAgent() returns true", "Mock dependencies"],
      "steps": ["Call triggerAgent()", "Verify usageMeterService.recordAgentRun() called with correct agentType"],
      "expectedResult": "Usage counter incremented for the agent type",
      "priority": "High"
    },
    {
      "id": "TC-SPRINT-001",
      "title": "SimulatedSprint runs 5 agents and returns all steps",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-011"],
      "targetComponents": ["SimulatedSprintService", "LLMAgentClient", "QualityGateService"],
      "preconditions": ["Project exists", "StubLLMAgentClient active (no DeepSeek key)"],
      "steps": ["POST /api/projects/{id}/simulated-sprint with changeDescription", "Verify response has 5 steps", "Verify all steps have status=COMPLETED and simulation=true", "Verify gateStatus computed", "Verify testResult and complianceResult summaries present"],
      "expectedResult": "5-step pipeline completes; gates evaluated; results persisted",
      "priority": "High"
    },
    {
      "id": "TC-SPRINT-002",
      "title": "SimulatedSprint rejects empty changeDescription",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-012"],
      "targetComponents": ["SimulatedSprintService"],
      "preconditions": ["Project exists"],
      "steps": ["Call runSprint(id, {branch:'main', changeDescription:''})", "Expect IllegalArgumentException"],
      "expectedResult": "Exception thrown with 'changeDescription is required'",
      "priority": "High"
    },
    {
      "id": "TC-SPRINT-003",
      "title": "SimulatedSprint rejects non-existent project",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-012"],
      "targetComponents": ["SimulatedSprintService"],
      "preconditions": ["projectService.getProjectById() returns null"],
      "steps": ["Call runSprint(id, req)", "Expect IllegalArgumentException"],
      "expectedResult": "Exception thrown with 'Project not found'",
      "priority": "High"
    },
    {
      "id": "TC-SPRINT-004",
      "title": "SimulatedSprint defaults branch to main",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-013"],
      "targetComponents": ["SimulatedSprintService"],
      "preconditions": ["Project exists", "Request with branch=null"],
      "steps": ["Call runSprint with branch=null", "Verify response.branch = 'main'"],
      "expectedResult": "Branch defaults to 'main'",
      "priority": "Low"
    },
    {
      "id": "TC-SPRINT-005",
      "title": "Sprint persists TestResult with simulation=true and branch",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-014"],
      "targetComponents": ["SimulatedSprintService", "TestResultRepository"],
      "preconditions": ["Project exists", "StubLLMAgentClient active"],
      "steps": ["Run simulated sprint", "Query testResultRepository for correlationId", "Verify testResult.simulation=true", "Verify testResult.branch matches input"],
      "expectedResult": "TestResult saved with correct simulation flag and branch",
      "priority": "High"
    },
    {
      "id": "TC-SPRINT-006",
      "title": "Sprint persists ComplianceResult with simulation=true",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-014"],
      "targetComponents": ["SimulatedSprintService", "ComplianceResultRepository"],
      "preconditions": ["Project exists", "StubLLMAgentClient active"],
      "steps": ["Run simulated sprint", "Query complianceResultRepository for correlationId", "Verify complianceResult.simulation=true"],
      "expectedResult": "ComplianceResult saved with simulation=true",
      "priority": "High"
    },
    {
      "id": "TC-SPRINT-007",
      "title": "Sprint evaluates all three quality gates",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-015"],
      "targetComponents": ["SimulatedSprintService", "QualityGateService"],
      "preconditions": ["Mocks for all dependencies", "StubLLMAgentClient active"],
      "steps": ["Run sprint with mocked services", "Verify qualityGateService.evaluateTestGate called", "Verify evaluateComplianceGate called", "Verify evaluateCoordinatedGate called", "Verify getGateStatus called"],
      "expectedResult": "All three gate evaluation methods invoked with correct parameters",
      "priority": "High"
    },
    {
      "id": "TC-SPRINT-008",
      "title": "Sprint response includes structured BA/Architect/Dev summaries",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-016"],
      "targetComponents": ["SimulatedSprintService"],
      "preconditions": ["Mocks for all dependencies", "Stub responses return parseable JSON"],
      "steps": ["Run sprint with mocked services", "Verify response.baSummary has requirementCount and requirementsSummary", "Verify response.architectSummary has componentCount and feasible", "Verify response.devSummary has fileChangeCount and riskLevel"],
      "expectedResult": "All three structured summaries populated from parsed DTOs",
      "priority": "Medium"
    },
    {
      "id": "TC-SPRINT-009",
      "title": "parseOrFallback handles malformed JSON without crashing",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-017"],
      "targetComponents": ["SimulatedSprintService"],
      "preconditions": ["Invalid JSON returned from LLM client"],
      "steps": ["Mock LLMAgentClient to return '{broken json'", "Run sprint", "Verify sprint completes (no exception)", "Verify empty DTO used as fallback"],
      "expectedResult": "Sprint completes successfully with empty fallback DTO; warning logged",
      "priority": "BusinessCritical"
    },
    {
      "id": "TC-SPRINT-010",
      "title": "parseOrFallback handles null LLM response",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-017"],
      "targetComponents": ["SimulatedSprintService"],
      "preconditions": ["Null returned from LLM client"],
      "steps": ["Mock LLMAgentClient to return null", "Run sprint", "Verify sprint completes with fallback empty DTO"],
      "expectedResult": "Sprint completes without NPE; fallback used",
      "priority": "High"
    },
    {
      "id": "TC-SPRINT-011",
      "title": "parseOrFallback handles empty string response",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-017"],
      "targetComponents": ["SimulatedSprintService"],
      "preconditions": ["Empty string returned from LLM client"],
      "steps": ["Mock LLMAgentClient to return empty string", "Run sprint", "Verify sprint completes with fallback"],
      "expectedResult": "Sprint completes; empty DTO used",
      "priority": "Medium"
    },
    {
      "id": "TC-SPRINT-012",
      "title": "parseOrFallback handles partial JSON with some valid fields",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-017", "UCTO-REQ-030"],
      "targetComponents": ["SimulatedSprintService"],
      "preconditions": ["Partial JSON with ignoreUnknown fields"],
      "steps": ["Mock LLM to return partial JSON with extra unknown fields", "Run sprint", "Verify parse succeeds for known fields", "Verify unknown fields ignored"],
      "expectedResult": "Partial JSON parsed; unknown fields ignored; sprint continues",
      "priority": "Medium"
    },
    {
      "id": "TC-DEEPSEEK-001",
      "title": "DeepSeek sends correct request format",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-018"],
      "targetComponents": ["DeepSeekAgentClient", "PromptCatalog"],
      "preconditions": ["Mock RestTemplate", "Mock PromptCatalog returns test prompt"],
      "steps": ["Call execute('BA_REQUIREMENTS', {projectTitle:'Test'})", "Capture request body", "Verify model, messages[], auth header, temperature, max_tokens"],
      "expectedResult": "Correctly formatted POST to DeepSeek API",
      "priority": "High"
    },
    {
      "id": "TC-DEEPSEEK-002",
      "title": "DeepSeek parses response content correctly",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-019"],
      "targetComponents": ["DeepSeekAgentClient"],
      "preconditions": ["Mock RestTemplate returns valid DeepSeek response"],
      "steps": ["Mock response with choices[0].message.content", "Call execute()", "Verify returned string equals expected content"],
      "expectedResult": "Content extracted from choices[0].message.content",
      "priority": "High"
    },
    {
      "id": "TC-DEEPSEEK-003",
      "title": "DeepSeek handles HTTP 429 gracefully",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-020"],
      "targetComponents": ["DeepSeekAgentClient"],
      "preconditions": ["Mock RestTemplate returns 429 Too Many Requests"],
      "steps": ["Mock exchange() → 429", "Call execute()", "Verify returned JSON contains 'error' with status code"],
      "expectedResult": "Error JSON returned; no exception thrown",
      "priority": "High"
    },
    {
      "id": "TC-DEEPSEEK-004",
      "title": "DeepSeek handles HTTP 500 gracefully",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-020"],
      "targetComponents": ["DeepSeekAgentClient"],
      "preconditions": ["Mock RestTemplate returns 500 Internal Server Error"],
      "steps": ["Mock exchange() → 500", "Call execute()", "Verify error JSON contains '500'"],
      "expectedResult": "Error JSON returned; no uncaught exception",
      "priority": "Medium"
    },
    {
      "id": "TC-DEEPSEEK-005",
      "title": "DeepSeek handles connection timeout",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-020"],
      "targetComponents": ["DeepSeekAgentClient"],
      "preconditions": ["Mock RestTemplate throws connection timeout"],
      "steps": ["Mock exchange() → throws RuntimeException('timed out')", "Call execute()", "Verify error JSON contains timeout message"],
      "expectedResult": "Error JSON with timeout message; no uncaught exception",
      "priority": "Medium"
    },
    {
      "id": "TC-DEEPSEEK-006",
      "title": "DeepSeek handles missing choices array",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-021"],
      "targetComponents": ["DeepSeekAgentClient"],
      "preconditions": ["Mock RestTemplate returns response without choices"],
      "steps": ["Mock exchange() → 200 with body '{\"id\":\"x\"}'", "Call execute()", "Verify raw JSON returned as fallback"],
      "expectedResult": "Raw response returned; no NPE",
      "priority": "High"
    },
    {
      "id": "TC-DEEPSEEK-007",
      "title": "DeepSeek handles empty choices array",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-021"],
      "targetComponents": ["DeepSeekAgentClient"],
      "preconditions": ["Mock RestTemplate returns response with empty choices"],
      "steps": ["Mock exchange() → 200 with body '{\"choices\":[]}'", "Call execute()", "Verify raw JSON returned as fallback"],
      "expectedResult": "Raw response returned; no IndexOutOfBoundsException",
      "priority": "Medium"
    },
    {
      "id": "TC-DEEPSEEK-008",
      "title": "DeepSeek handles non-JSON response body",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-021"],
      "targetComponents": ["DeepSeekAgentClient"],
      "preconditions": ["Mock RestTemplate returns non-JSON response"],
      "steps": ["Mock exchange() → 200 with body 'not json'", "Call execute()", "Verify raw body returned as fallback"],
      "expectedResult": "Non-JSON body returned as-is without parsing failure",
      "priority": "Medium"
    },
    {
      "id": "TC-DEEPSEEK-009",
      "title": "DeepSeek unavailable without API key",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-022"],
      "targetComponents": ["DeepSeekAgentClient"],
      "preconditions": ["API key set to empty string"],
      "steps": ["Set apiKey = ''", "Call isAvailable() → false", "Call execute()", "Verify error JSON contains 'not configured'"],
      "expectedResult": "isAvailable() false; execute returns error without HTTP call",
      "priority": "High"
    },
    {
      "id": "TC-DEEPSEEK-010",
      "title": "Conditional bean wiring: DeepSeek active when key set",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-023"],
      "targetComponents": ["LLMAgentClient", "DeepSeekAgentClient", "StubLLMAgentClient"],
      "preconditions": ["Application context with DEEPSEEK_API_KEY set"],
      "steps": ["Load application context with key", "Verify DeepSeekAgentClient bean present", "Verify StubLLMAgentClient not present"],
      "expectedResult": "DeepSeekAgentClient active; Stub inactive",
      "priority": "High"
    },
    {
      "id": "TC-DEEPSEEK-011",
      "title": "Conditional bean wiring: Stub active when key absent",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-023"],
      "targetComponents": ["LLMAgentClient", "DeepSeekAgentClient", "StubLLMAgentClient"],
      "preconditions": ["Application context without DEEPSEEK_API_KEY"],
      "steps": ["Load application context without key", "Verify StubLLMAgentClient bean present", "Verify DeepSeekAgentClient not present"],
      "expectedResult": "Stub active; DeepSeek inactive",
      "priority": "High"
    },
    {
      "id": "TC-DEEPSEEK-012",
      "title": "DeepSeek context includes all project info in user message",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-024"],
      "targetComponents": ["DeepSeekAgentClient"],
      "preconditions": ["Mock RestTemplate", "Mock PromptCatalog"],
      "steps": ["Call execute with context: projectTitle, changeDescription, requirements", "Capture request body", "Verify all context values present in user message block"],
      "expectedResult": "All fields propagated to user message",
      "priority": "Medium"
    },
    {
      "id": "TC-DTO-001",
      "title": "BaRequirementsOutput parses valid JSON correctly",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-025"],
      "targetComponents": ["BaRequirementsOutput"],
      "preconditions": ["Valid JSON string"],
      "steps": ["Parse valid JSON with all fields", "Verify id, area, description, risk, qualityAttributes, openQuestions"],
      "expectedResult": "All fields correctly deserialized",
      "priority": "High"
    },
    {
      "id": "TC-DTO-002",
      "title": "BaRequirementsOutput handles malformed JSON",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-025"],
      "targetComponents": ["BaRequirementsOutput"],
      "preconditions": ["Invalid JSON string"],
      "steps": ["Attempt to parse '{broken'", "Expect exception"],
      "expectedResult": "Jackson throws parse exception; caller can use fallback",
      "priority": "Medium"
    },
    {
      "id": "TC-DTO-003",
      "title": "BaRequirementsOutput ignores unknown fields",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-025", "UCTO-REQ-030"],
      "targetComponents": ["BaRequirementsOutput"],
      "preconditions": ["JSON with extra unknown fields"],
      "steps": ["Parse JSON with unexpected fields", "Verify no exception", "Verify known fields populated"],
      "expectedResult": "Extra fields silently ignored",
      "priority": "Medium"
    },
    {
      "id": "TC-DTO-004",
      "title": "BaRequirementsOutput handles empty arrays and nulls",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-025"],
      "targetComponents": ["BaRequirementsOutput"],
      "preconditions": ["JSON with empty requirements array"],
      "steps": ["Parse '{}' and '{\"requirements\":[]}'", "Verify empty lists returned, no NPE"],
      "expectedResult": "Empty lists handled; no null pointer",
      "priority": "Medium"
    },
    {
      "id": "TC-DTO-005",
      "title": "ArchitectDesignOutput parses valid JSON",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-026"],
      "targetComponents": ["ArchitectDesignOutput"],
      "preconditions": ["Valid JSON with components, services, dataFlows"],
      "steps": ["Parse valid architect JSON", "Verify components, services, dataFlows, feasibility, assumptions"],
      "expectedResult": "All fields parsed correctly",
      "priority": "High"
    },
    {
      "id": "TC-DTO-006",
      "title": "DevImplementOutput parses valid JSON",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-027"],
      "targetComponents": ["DevImplementOutput"],
      "preconditions": ["Valid JSON with filesToChange, rationale, riskLevel"],
      "steps": ["Parse valid dev JSON", "Verify filesToChange, rationale, riskLevel, testCoverage"],
      "expectedResult": "All file change and risk fields parsed",
      "priority": "High"
    },
    {
      "id": "TC-DTO-007",
      "title": "TesterOutput parses valid test results",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-028"],
      "targetComponents": ["TesterOutput"],
      "preconditions": ["Valid JSON with testsRun, passed, failed, coverage, failures"],
      "steps": ["Parse valid tester JSON", "Verify all metrics and failures list"],
      "expectedResult": "All test metrics and failure details correctly parsed",
      "priority": "High"
    },
    {
      "id": "TC-DTO-008",
      "title": "TesterOutput handles missing optional fields",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-028"],
      "targetComponents": ["TesterOutput"],
      "preconditions": ["JSON without optional fields"],
      "steps": ["Parse '{\"testsRun\":0,\"testsPassed\":0,\"testsFailed\":0,\"coveragePercent\":0}'", "Verify defaults for testsSkipped=0, failures=[], doDMet=false"],
      "expectedResult": "Missing optional fields default to safe values",
      "priority": "Medium"
    },
    {
      "id": "TC-DTO-009",
      "title": "ComplianceOutput parses valid compliance JSON",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-029"],
      "targetComponents": ["ComplianceOutput"],
      "preconditions": ["Valid JSON with overallStatus, severity, checks"],
      "steps": ["Parse valid compliance JSON", "Verify overallStatus, severity, checksPassed, checksFailed, findings"],
      "expectedResult": "Compliance fields correctly parsed",
      "priority": "High"
    },
    {
      "id": "TC-DTO-010",
      "title": "ComplianceOutput handles null/missing fields",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-029", "UCTO-REQ-030"],
      "targetComponents": ["ComplianceOutput"],
      "preconditions": ["JSON with missing optional fields"],
      "steps": ["Parse '{}'", "Verify checksPassed=[], checksFailed=[], findings=[], humanQuestions=[], no NPE"],
      "expectedResult": "Missing fields default to empty collections",
      "priority": "Medium"
    },
    {
      "id": "TC-AGENT-001",
      "title": "StubLLMAgentClient returns valid JSON for all 5 prompt keys",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-031"],
      "targetComponents": ["StubLLMAgentClient", "PromptCatalog"],
      "preconditions": ["PromptCatalog loaded with all 5 keys"],
      "steps": ["Execute each of 5 prompt keys", "Verify each returns valid parseable JSON"],
      "expectedResult": "All 5 return valid JSON matching expected schemas",
      "priority": "High"
    },
    {
      "id": "TC-AGENT-002",
      "title": "StubLLMAgentClient returns error for unknown key",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-031"],
      "targetComponents": ["StubLLMAgentClient"],
      "preconditions": ["PromptCatalog does not have key"],
      "steps": ["Call execute('UNKNOWN', null)", "Verify error JSON returned"],
      "expectedResult": "Error JSON with 'Unknown prompt key'",
      "priority": "Low"
    },
    {
      "id": "TC-AGENT-003",
      "title": "PromptCatalog loads YAML and supports variable substitution",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-032"],
      "targetComponents": ["PromptCatalog"],
      "preconditions": ["agent_prompts.yaml exists on classpath"],
      "steps": ["Initialize PromptCatalog", "Call getPrompt('BA_REQUIREMENTS', {projectTitle:'Test'})", "Verify substituted prompt contains 'Test'"],
      "expectedResult": "All 5 prompts loaded; variable substitution works",
      "priority": "High"
    },
    {
      "id": "TC-AGENT-004",
      "title": "PromptCatalog returns null for missing key",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-032"],
      "targetComponents": ["PromptCatalog"],
      "preconditions": ["Catalog loaded"],
      "steps": ["Call getPrompt('NONEXISTENT')", "Verify null returned"],
      "expectedResult": "Null for unknown keys",
      "priority": "Low"
    },
    {
      "id": "TC-PERSIST-001",
      "title": "TestResult entity round-trips through repository",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-033"],
      "targetComponents": ["TestResultRepository"],
      "preconditions": ["Database available"],
      "steps": ["Create TestResult with all fields", "Save", "Query by id", "Verify all fields match"],
      "expectedResult": "All fields persisted and retrieved correctly",
      "priority": "High"
    },
    {
      "id": "TC-PERSIST-002",
      "title": "ComplianceResult entity round-trips through repository",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-034"],
      "targetComponents": ["ComplianceResultRepository"],
      "preconditions": ["Database available"],
      "steps": ["Create ComplianceResult with all fields", "Save", "Query by id", "Verify all fields match"],
      "expectedResult": "All fields persisted and retrieved correctly",
      "priority": "High"
    },
    {
      "id": "TC-PERSIST-003",
      "title": "GateEvaluation entity round-trips with query by project+branch+type",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-035"],
      "targetComponents": ["GateEvaluationRepository"],
      "preconditions": ["Database available", "GateEvaluation records exist"],
      "steps": ["Save GateEvaluation", "Query findTopByProjectIdAndGateTypeAndBranch", "Verify correct record returned"],
      "expectedResult": "Most recent gate evaluation returned by query",
      "priority": "Medium"
    },
    {
      "id": "TC-PERSIST-004",
      "title": "V6 Flyway migration applies without errors",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-036"],
      "targetComponents": ["Flyway migrations"],
      "preconditions": ["Empty database"],
      "steps": ["Start application with flyway enabled", "Verify all 6 migrations applied", "Verify test_results, compliance_results, gate_evaluations exist", "Verify audit_logs has simulation column"],
      "expectedResult": "All migrations apply; schema matches design",
      "priority": "High"
    },
    {
      "id": "TC-PERSIST-005",
      "title": "V6 migration adds simulation column to existing audit_logs",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-036"],
      "targetComponents": ["V6 migration"],
      "preconditions": ["Database with existing audit_logs without simulation column"],
      "steps": ["Run V6 migration", "Verify simulation column added with default FALSE"],
      "expectedResult": "Column added with default false; existing rows preserved",
      "priority": "Medium"
    },
    {
      "id": "TC-PERSIST-006",
      "title": "RepoWorkspaceService creates workspace directory structure",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-037"],
      "targetComponents": ["RepoWorkspaceService"],
      "preconditions": ["Temporary directory for workspace base"],
      "steps": ["Set workspace base to temp dir", "Call getWorkspaceDir and getSourceDir", "Verify path structure"],
      "expectedResult": "Correct {base}/{projectId}/source path",
      "priority": "Low"
    },
    {
      "id": "TC-SIM-001",
      "title": "Simulation flag propagates from event to RepoWorkspaceService",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-041"],
      "targetComponents": ["AgentEventListener", "RepoWorkspaceService"],
      "preconditions": ["Event payload with simulation=true"],
      "steps": ["Send agent.developer.trigger with simulation=true", "Verify handleDeveloperTrigger calls prepareWorkspace(project, true)"],
      "expectedResult": "Simulation flag extracted; passed to RepoWorkspaceService",
      "priority": "High"
    },
    {
      "id": "TC-SIM-002",
      "title": "Simulation mode skips real git operations",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-041"],
      "targetComponents": ["RepoWorkspaceService"],
      "preconditions": ["Project with valid repoUrl", "Mock AuditLogService"],
      "steps": ["Call prepareWorkspace(project, simulation=true)", "Verify audit log with 'REPO_PREPARE_SIMULATED'", "Verify source dir created but no git clone"],
      "expectedResult": "Source dir created; git not invoked; simulated audit logged",
      "priority": "High"
    },
    {
      "id": "TC-SIM-003",
      "title": "Simulation flag defaults to false when absent",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-042"],
      "targetComponents": ["AgentEventListener"],
      "preconditions": ["Event payload without simulation field"],
      "steps": ["Call isSimulation(payload without simulation)", "Verify returns false"],
      "expectedResult": "Default is false when flag absent",
      "priority": "High"
    },
    {
      "id": "TC-SIM-004",
      "title": "Quality gate simulation mode uses _SIMULATED audit suffix",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-038", "UCTO-REQ-041"],
      "targetComponents": ["QualityGateService"],
      "preconditions": ["simulation=true flag"],
      "steps": ["Call evaluateTestGate with simulation=true", "Verify audit log action contains '_SIMULATED'"],
      "expectedResult": "Simulated action suffix used; entity marked simulated",
      "priority": "High"
    },
    {
      "id": "TC-FRONT-001",
      "title": "Quality & Agents tab loads gate status for project",
      "type": "e2e",
      "requirementIds": ["UCTO-REQ-039"],
      "targetComponents": ["ProjectDetailScreen (Flutter)", "QualityGateService"],
      "preconditions": ["Frontend running", "Project with gate evaluations exists"],
      "steps": ["Navigate to project detail", "Tap 'Quality & Agents' tab", "Verify gate status cards render", "Verify pass/fail chips display correctly"],
      "expectedResult": "Gate status loaded and displayed; no loading spinner remains",
      "priority": "Medium"
    },
    {
      "id": "TC-FRONT-002",
      "title": "Simulation indicator badge shows for simulated gates",
      "type": "e2e",
      "requirementIds": ["UCTO-REQ-039"],
      "targetComponents": ["ProjectDetailScreen (Flutter)"],
      "preconditions": ["Gate evaluations exist with simulation=true"],
      "steps": ["Open Quality & Agents tab", "Verify SIM badge displayed on gate cards"],
      "expectedResult": "SIM badge visible when simulation=true",
      "priority": "Medium"
    },
    {
      "id": "TC-FRONT-003",
      "title": "Run Simulated Sprint button triggers flow and refreshes",
      "type": "e2e",
      "requirementIds": ["UCTO-REQ-039", "UCTO-REQ-011"],
      "targetComponents": ["ProjectDetailScreen (Flutter)", "SimulatedSprintService"],
      "preconditions": ["Frontend running", "Project exists"],
      "steps": ["Navigate to Quality & Agents tab", "Tap 'Run Simulated Sprint'", "Wait for completion", "Verify gate status cards update"],
      "expectedResult": "Sprint triggered; gate status refreshes automatically",
      "priority": "Medium"
    },
    {
      "id": "TC-SEC-001",
      "title": "Non-owner cannot update repo config",
      "type": "integration",
      "requirementIds": ["UCTO-REQ-002", "UCTO-REQ-004"],
      "targetComponents": ["ProjectController"],
      "preconditions": ["Two users: owner and non-owner", "Project exists"],
      "steps": ["Non-owner PUT /api/projects/{id}/repo → expect 403"],
      "expectedResult": "Non-owner receives 403 Forbidden",
      "priority": "High"
    },
    {
      "id": "TC-RESILIENCE-001",
      "title": "All 5 DTOs handle malformed JSON without crashing",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-025", "UCTO-REQ-026", "UCTO-REQ-027", "UCTO-REQ-028", "UCTO-REQ-029"],
      "targetComponents": ["All agent DTOs"],
      "preconditions": ["Malformed JSON strings for each DTO"],
      "steps": ["Attempt Jackson parse with malformed JSON for each of 5 DTOs", "Verify each throws parse exception"],
      "expectedResult": "All 5 DTOs throw parseable exception on bad JSON; no hangs or crashes",
      "priority": "High"
    },
    {
      "id": "TC-RESILIENCE-002",
      "title": "Gate rejection loop: failed test → blocked coordinated gate",
      "type": "unit",
      "requirementIds": ["UCTO-REQ-015"],
      "targetComponents": ["QualityGateService"],
      "preconditions": ["Test gate fails, compliance passes"],
      "steps": ["Evaluate test gate with failures", "Evaluate compliance gate as pass", "Evaluate coordinated gate", "Verify coordinated gate fails"],
      "expectedResult": "Coordinated gate blocked when test gate fails",
      "priority": "High"
    }
  ],
  "untestedRequirements": [
    {
      "id": "UCTO-REQ-008",
      "description": "Bootstrap project POST /api/orchestrator/bootstrap — covered by integration test TC-API-014 but not e2e against real filesystem",
      "gap": "Integration test uses mocks; no real filesystem verification"
    },
    {
      "id": "UCTO-REQ-010",
      "description": "Usage metering with 402 at tier limit — covered by unit tests TC-API-017/018 but no e2e with real Chargebee",
      "gap": "No end-to-end test with real Chargebee integration"
    },
    {
      "id": "UCTO-REQ-037",
      "description": "RepoWorkspaceService directory structure — trivial verification TC-PERSIST-006",
      "gap": "Low risk; path structure verified"
    },
    {
      "id": "UCTO-REQ-039 partial",
      "description": "Frontend Quality & Agents tab — e2e tests TC-FRONT-001/002/003 require running Flutter app",
      "gap": "E2E tests defined but not automated in current CI"
    }
  ],
  "coverageSummary": {
    "totalRequirements": 42,
    "coveredRequirements": 38,
    "coveragePercent": 90.5,
    "partialCoverage": 4,
    "testClassification": {
      "unit": 45,
      "integration": 21,
      "e2e": 3,
      "non-functional": 0,
      "total": 69
    },
    "priorityBreakdown": {
      "High": 38,
      "Medium": 25,
      "Low": 6
    },
    "llmResilienceSpecificTests": 18,
    "simulationRelatedTests": 5
  }
}
